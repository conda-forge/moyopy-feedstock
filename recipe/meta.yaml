{% set name = "moyopy" %}
{% set version = "0.7.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/moyopy-{{ version }}.tar.gz
  sha256: 57b7928d13ebf8b1f8c5bcf8c7f03092de45d0b948e9916b8140b5a3100e822e

build:
  script:
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
  number: 0

requirements:
  build:
    - python  # [build_platform != target_platform]
    - cross-python_{{ target_platform }}  # [build_platform != target_platform]
    - maturin >=1.0,<2.0  # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python
    - maturin >=1.0,<2.0
    - pip
  run:
    - python
    - typing_extensions

test:
  imports:
    - moyopy
  commands:
    - pip check
  requires:
    - pip

about:
  summary: Python binding of Moyo
  home: https://github.com/spglib/moyo
  license: Apache-2.0
  license_file: LICENSE-MIT

extra:
  recipe-maintainers:
    - ocefpaf
